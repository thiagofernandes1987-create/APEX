# UCO-Sensor API — Python dependencies
# ======================================
# Instalar com: pip install -r requirements.txt
#
# Categorias:
#   [core]      — obrigatórias para o FrequencyEngine funcionar
#   [parsers]   — tree-sitter para análise multi-linguagem
#   [optional]  — melhoram precisão mas não são obrigatórias
#
# Python mínimo: 3.9+  (recomendado: 3.11)

# ── [core] Cálculo numérico e espectral ──────────────────────────────────────
numpy>=1.24.0
scipy>=1.10.0
PyWavelets>=1.4.0

# ── [parsers] tree-sitter multi-linguagem ────────────────────────────────────
# API principal (versão >= 0.21 usa Language(grammar.language()))
tree-sitter>=0.21.0

# Gramáticas por linguagem (instalar as que precisar)
tree-sitter-python>=0.21.0
tree-sitter-javascript>=0.21.0
tree-sitter-typescript>=0.21.0
tree-sitter-java>=0.21.0
tree-sitter-go>=0.21.0

# ── [optional] Métricas adicionais ───────────────────────────────────────────
# statsmodels       # modelos ARIMA para análise de tendência avançada
# scikit-learn      # clustering adicional para DBSCANDiscovery

# ── [dev / test] ──────────────────────────────────────────────────────────────
# pytest>=7.0.0
# pytest-cov>=4.0.0
