patch_inviolable_rules:
executor: LLM_BEHAVIOR  # backfilled by lint_executor (SR_40)
  additions:
    scientific_rules:
    - 'SR_07: [POT_GUARD] Todo PoT program deve ter STEP_0 do tipo FMEA_GUARD. Ausência = output inválido
      com confidence_cap 45.

      '
    - 'SR_08: [MENTAL_INTERPRETER] Toda execução de Specialized Skill deve passar pelo mental_interpreter.
      Execução direta (sem trace) não é permitida em modos DEEP/RESEARCH/SCIENTIFIC.

      '
  enforcement_additions:
    SR_07:
    - pot_engine.program_validation
    SR_08:
    - specialized_skill_engine.pre_execution
