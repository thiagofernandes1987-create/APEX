{
 "_meta": {
  "note": "Tool-use memory: every capability the runtime can wield — runtime tools (44 syscalls + CLI), installed skills (SKILL.md commands/triggers), design templates — plus the environment probe (languages/libraries actually present). Mapping documents commands; it NEVER executes them (gates/H5 still govern). record_use() feeds real outcomes into learning so promotion is earned. Rebuild after every install.",
  "count": 79
 },
 "environment": {
  "python": "python 3.11.15",
  "tools": {
   "python": true,
   "python3": true,
   "node": true,
   "npx": true,
   "npm": true,
   "git": true,
   "pip": true,
   "docker": true,
   "bash": true,
   "pwsh": false,
   "powershell": false,
   "gh": false
  },
  "libraries": {
   "numpy": false,
   "scipy": false,
   "sklearn": false,
   "sympy": false,
   "pandas": false,
   "yaml": true,
   "matplotlib": false,
   "sentence_transformers": false,
   "requests": true
  },
  "os": "posix",
  "platform": "linux"
 },
 "capabilities": [
  {
   "id": "tool:_ast_helpers",
   "kind": "runtime-tool",
   "name": "_ast_helpers",
   "path": "scripts/_ast_helpers.py",
   "description": "_ast_helpers.py — shared AST-scan primitives for the security gates. WHY THIS EXISTS: `skill_scout.ast_security_scan` and `guards.forge_load_gate` both need the same low-level checks (dangerous `open()` modes, deserializer RCE sinks). Befor",
   "triggers": "",
   "commands": [],
   "io": {
    "send": "ver docstring/CLI",
    "receive": "ver docstring",
    "apply_when": "From any static gate that walks a `ast` tree of untrusted code before staging/exec."
   }
  },
  {
   "id": "tool:_tfidf",
   "kind": "runtime-tool",
   "name": "_tfidf",
   "path": "scripts/_tfidf.py",
   "description": "_tfidf.py — Pure-Python TF-IDF + cosine fallback (no sklearn required). WHY THIS EXISTS: router/gravity/agent_registry used sklearn's TfidfVectorizer as a HARD dependency, so the orchestrator (the skill's entry point) crashed with ModuleNot",
   "triggers": "",
   "commands": [],
   "io": {
    "send": "ver docstring/CLI",
    "receive": "ver docstring",
    "apply_when": "imported by router.py / gravity.py / agent_registry.py when sklearn is missing."
   }
  },
  {
   "id": "tool:agent_lifecycle",
   "kind": "runtime-tool",
   "name": "agent_lifecycle",
   "path": "scripts/agent_lifecycle.py",
   "description": "agent_lifecycle.py — the CLOSED-LOOP agent pipeline: dissect → competence matrix → find-or-create agent → validate/equip skills (discovery cascade) → executable spec → (host executes) → evolve. WHY THIS EXISTS (the author's O-2 flow, made r",
   "triggers": "",
   "commands": [
    "python scripts/agent_lifecycle.py"
   ],
   "io": {
    "send": "ver docstring/CLI",
    "receive": "ver docstring",
    "apply_when": ""
   }
  },
  {
   "id": "tool:agent_materializer",
   "kind": "runtime-tool",
   "name": "agent_materializer",
   "path": "scripts/agent_materializer.py",
   "description": "agent_materializer.py — turn a VALIDATED generic spawn into a STANDARDIZED, durable specialist artifact, so the prompt library self-evolves across sessions (the author's auto-evolutive vision). WHY THIS EXISTS: agent_lifecycle can SYNTHESIZ",
   "triggers": "",
   "commands": [
    "python scripts/agent_materializer.py"
   ],
   "io": {
    "send": "ver docstring/CLI",
    "receive": "ver docstring",
    "apply_when": ""
   }
  },
  {
   "id": "tool:agent_registry",
   "kind": "runtime-tool",
   "name": "agent_registry",
   "path": "scripts/agent_registry.py",
   "description": "agent_registry.py — Agent roster + competence mapping (APEX method). WHY THIS EXISTS: APEX reasons through personas (\"agents\"). This registry keeps, for each persona: its personality, its specialization, and WHICH SKILLS it is competent to ",
   "triggers": "",
   "commands": [
    "python scripts/agent_registry.py"
   ],
   "io": {
    "send": "ver docstring/CLI",
    "receive": "ver docstring",
    "apply_when": ""
   }
  },
  {
   "id": "tool:agent_spawn",
   "kind": "runtime-tool",
   "name": "agent_spawn",
   "path": "scripts/agent_spawn.py",
   "description": "agent_spawn.py — the SPAWN CONTRACT: turn a roster persona into a fully-equipped, executable agent spec (RT-19/RT-27, the author's design). WHY THIS EXISTS: The 213-entry roster is deliberately LEAN metadata — the differentiator is not ship",
   "triggers": "",
   "commands": [
    "python scripts/agent_spawn.py"
   ],
   "io": {
    "send": "agent_id + task + mode + stance",
    "receive": "AgentSpec executável {instruction, skills, context, spawn_ready}",
    "apply_when": "antes de todo fan-out Level-B"
   }
  },
  {
   "id": "tool:apex_st_metric",
   "kind": "runtime-tool",
   "name": "apex_st_metric",
   "path": "scripts/apex_st_metric.py",
   "description": "apex_st_metric.py — APEX apex_st_metric (v00.32.1, OPP-MAS-06), nativized. WHY: without this, APEX can't tell whether it's progressing or running in circles. It measures the distance between consecutive session snapshots in information spac",
   "triggers": "",
   "commands": [
    "python scripts/apex_st_metric.py"
   ],
   "io": {
    "send": "ver docstring/CLI",
    "receive": "ver docstring",
    "apply_when": "STEP_13."
   }
  },
  {
   "id": "tool:asset_manager",
   "kind": "runtime-tool",
   "name": "asset_manager",
   "path": "scripts/asset_manager.py",
   "description": "asset_manager.py — APEX manages & routes all mined assets. WHY: the full APEX repo yields (a) APEX-native assets (UCO, UCO-Sensor) that are nativized, and (b) 39 third-party assets + 23 MCP servers that are INDEXED (not copied). This lets A",
   "triggers": "",
   "commands": [
    "python scripts/asset_manager.py"
   ],
   "io": {
    "send": "ver docstring/CLI",
    "receive": "ver docstring",
    "apply_when": "Call at skill-resolution time to route a need across managed assets + MCPs."
   }
  },
  {
   "id": "tool:attraction_graph",
   "kind": "runtime-tool",
   "name": "attraction_graph",
   "path": "scripts/attraction_graph.py",
   "description": "attraction_graph.py — PRECOMPUTED gravitational routing graph (the author's \"super-estrutura\"). WHY THIS EXISTS: gravity.py recomputes TF-IDF attraction on every call — good for one task, wasteful for the ecosystem. The author's design: a R",
   "triggers": "",
   "commands": [
    "python scripts/attraction_graph.py"
   ],
   "io": {
    "send": "competência-semente ou texto da necessidade (equip_for)",
    "receive": "membros que se completam, ranqueados por atração",
    "apply_when": "compor ferramentas complementares sem re-descoberta"
   }
  },
  {
   "id": "tool:bayes",
   "kind": "runtime-tool",
   "name": "bayes",
   "path": "scripts/bayes.py",
   "description": "bayes.py — APEX Bayesian layer, made computable (was bayesian_curator LLM_BEHAVIOR). Implements the concrete machinery specified in the APEX kernel defaults: - beta-binomial conjugate update (class_1) -> posterior belief from evidence - Ome",
   "triggers": "",
   "commands": [
    "python scripts/bayes.py"
   ],
   "io": {
    "send": "priors + likelihoods (crenças do LLM)",
    "receive": "posterior + decisão Ω (ADOPT/REVIEW/REJECT) + R_acum",
    "apply_when": "convergência PMI / decisão entre hipóteses"
   }
  },
  {
   "id": "tool:capability_map",
   "kind": "runtime-tool",
   "name": "capability_map",
   "path": "scripts/capability_map.py",
   "description": "capability_map.py — the TOOL-USE MEMORY: map every capability the runtime can wield and REMEMBER how to extract the maximum from it (the author's item 2). WHY THIS EXISTS: Installed skills are dead text the LLM rediscovers every session. AP",
   "triggers": "",
   "commands": [
    "python scripts/capability_map.py"
   ],
   "io": {
    "send": "pergunta 'como faço X?'",
    "receive": "capacidade + comandos exatos + contrato de I/O",
    "apply_when": "antes de usar qualquer ferramenta"
   }
  },
  {
   "id": "tool:chaos_operators",
   "kind": "runtime-tool",
   "name": "chaos_operators",
   "path": "scripts/chaos_operators.py",
   "description": "chaos_operators.py — APEX ChaosOperators (DIFF_v22_001), nativized as an OFFENSIVE exploration engine. WHY THIS EXISTS: The chaos stance is not just a brake against premature convergence (SR_11) — from FOGGY up it is the INNOVATION driver t",
   "triggers": "",
   "commands": [
    "python scripts/chaos_operators.py"
   ],
   "io": {
    "send": "ver docstring/CLI",
    "receive": "ver docstring",
    "apply_when": "From FOGGY upward, per the exploration policy (config.exploration_policy). In RESEARCH the"
   }
  },
  {
   "id": "tool:code_genetics",
   "kind": "runtime-tool",
   "name": "code_genetics",
   "path": "scripts/code_genetics.py",
   "description": "code_genetics.py — APEX code_genetics / vaccine store (SR_35), nativized. WHY: learn from fixed errors. save_vaccine() crystallizes an (error_signature -> fix) pattern with O(1) frozenset lookup; error_evolution_loop applies known fixes and",
   "triggers": "",
   "commands": [
    "python scripts/code_genetics.py"
   ],
   "io": {
    "send": "ver docstring/CLI",
    "receive": "ver docstring",
    "apply_when": "After a PoT/code failure is fixed, to crystallize the error->fix pattern."
   }
  },
  {
   "id": "tool:competence_matrix",
   "kind": "runtime-tool",
   "name": "competence_matrix",
   "path": "scripts/competence_matrix.py",
   "description": "competence_matrix.py — APEX metacognition (Op3): the agent x domain heat-map + the persona/skill/rule diagnosis. WHY THIS EXISTS: When an agent's answer is weak, the system must know WHY: is it the wrong PERSONA (right domain, stuck perspec",
   "triggers": "",
   "commands": [
    "python scripts/competence_matrix.py"
   ],
   "io": {
    "send": "ver docstring/CLI",
    "receive": "ver docstring",
    "apply_when": "- concurrent_executor: to decide abort/replace and HOW (swap persona vs inject skill)."
   }
  },
  {
   "id": "tool:concurrent_executor",
   "kind": "runtime-tool",
   "name": "concurrent_executor",
   "path": "scripts/concurrent_executor.py",
   "description": "concurrent_executor.py — Parallel PoT-by-stance executor + PMI convergence (APEX cognitive parallelism, Level A). WHY THIS EXISTS: mental_interpreter promised a SPECULATION phase — N stances (optimistic / pessimistic / neutral / chaos) gene",
   "triggers": "",
   "commands": [
    "python scripts/concurrent_executor.py"
   ],
   "io": {
    "send": "stances [{name, persona, program}] ou hypotheses",
    "receive": "{merge, pmi, decision, restart?} (barrier + laudos SHA-256)",
    "apply_when": "rodada paralela DEEP+ (Level A) / painel de diretores"
   }
  },
  {
   "id": "tool:config",
   "kind": "runtime-tool",
   "name": "config",
   "path": "scripts/config.py",
   "description": "config.py — Persistent user preferences for the apex-method skill. WHY THIS EXISTS: The skill should remember the user's choices between sessions: which operating modes they prefer, which router backend to use, and how they want skill disco",
   "triggers": "",
   "commands": [
    "python scripts/config.py"
   ],
   "io": {
    "send": "ver docstring/CLI",
    "receive": "ver docstring",
    "apply_when": "Read at the start of a run (orchestrator picks the preferred mode when applicable);"
   }
  },
  {
   "id": "tool:curated",
   "kind": "runtime-tool",
   "name": "curated",
   "path": "scripts/curated.py",
   "description": "curated.py — Load the curated per-domain skill map and pre-assign skills to agents. WHY: the user picked the best skills per domain (programming, frontend, backend, design, ux-ui, engineering, mcp-servers, brainstorm, finance, math, science",
   "triggers": "",
   "commands": [
    "python scripts/curated.py"
   ],
   "io": {
    "send": "ver docstring/CLI",
    "receive": "ver docstring",
    "apply_when": ""
   }
  },
  {
   "id": "tool:deep_research",
   "kind": "runtime-tool",
   "name": "deep_research",
   "path": "scripts/deep_research.py",
   "description": "deep_research.py — Goal-like iterative deep research over the APEX domain and its agents. WHY THIS EXISTS: A \"goal\" in APEX terms is a research loop that runs in RESEARCH/SCIENTIFIC mode: dissect the question, route each part to the right A",
   "triggers": "",
   "commands": [
    "python scripts/deep_research.py"
   ],
   "io": {
    "send": "ver docstring/CLI",
    "receive": "ver docstring",
    "apply_when": "Multi-part, high-stakes, or exploratory questions where the user wants the pipeline to"
   }
  },
  {
   "id": "tool:execution_policy",
   "kind": "runtime-tool",
   "name": "execution_policy",
   "path": "scripts/execution_policy.py",
   "description": "execution_policy.py — explicit ROUTING CONTRACT + the 3-persona dissect ENTRY. WHY THIS EXISTS: The single biggest failure mode of a \"cognitive runtime\" is routing DISCOVERY/RESEARCH into the sealed PoT sandbox (no internet) — you lose skil",
   "triggers": "",
   "commands": [
    "python scripts/execution_policy.py"
   ],
   "io": {
    "send": "ver docstring/CLI",
    "receive": "ver docstring",
    "apply_when": ""
   }
  },
  {
   "id": "tool:federation",
   "kind": "runtime-tool",
   "name": "federation",
   "path": "scripts/federation.py",
   "description": "federation.py — o protótipo de FEDERAÇÃO: pacotes de conhecimento VALIDADO, assinados, circulando entre instâncias (o efeito de rede da visão do autor, desbloqueado pelo ledger multi-dispositivo). WHY THIS EXISTS: Cada instância do APEX apr",
   "triggers": "",
   "commands": [
    "python scripts/federation.py"
   ],
   "io": {
    "send": "ver docstring/CLI",
    "receive": "ver docstring",
    "apply_when": "- export_pack(): ao fim de um ciclo com aprendizado validado — gera o pacote para publicar"
   }
  },
  {
   "id": "tool:fractal_compression",
   "kind": "runtime-tool",
   "name": "fractal_compression",
   "path": "scripts/fractal_compression.py",
   "description": "fractal_compression.py — APEX fractal_hypothesis_compression (v00.20.0), nativized. WHY: at each fractal level the hypothesis space grows; without pruning, absurd/dominated hypotheses survive and waste the next level's budget. This prunes t",
   "triggers": "",
   "commands": [
    "python scripts/fractal_compression.py"
   ],
   "io": {
    "send": "ver docstring/CLI",
    "receive": "ver docstring",
    "apply_when": "after STEP_5 (post-interference), when fractal_depth>0 AND hypotheses_active>1."
   }
  },
  {
   "id": "tool:geodesic_scheduler",
   "kind": "runtime-tool",
   "name": "geodesic_scheduler",
   "path": "scripts/geodesic_scheduler.py",
   "description": "geodesic_scheduler.py — APEX geodesic_scheduler (v00.32.1, OPP-MAS-B7, SR_34), nativized. WHY: a fixed step order wastes tokens when the direction is already clear. This greedily orders steps by information-per-token (ΔH_cognitive / tokens_",
   "triggers": "",
   "commands": [
    "python scripts/geodesic_scheduler.py"
   ],
   "io": {
    "send": "ver docstring/CLI",
    "receive": "ver docstring",
    "apply_when": "cognitive_mode IN [DEEP, RESEARCH, SCIENTIFIC]."
   }
  },
  {
   "id": "tool:geometry_estimator",
   "kind": "runtime-tool",
   "name": "geometry_estimator",
   "path": "scripts/geometry_estimator.py",
   "description": "geometry_estimator.py — APEX geometry_estimator (v00.20.2), nativized. WHY: an LLM can't know how big an integration block can be before error blows up. This estimates the adaptive local error (DELTA_ERR) by step-doubling and derives the op",
   "triggers": "",
   "commands": [
    "python scripts/geometry_estimator.py"
   ],
   "io": {
    "send": "ver docstring/CLI",
    "receive": "ver docstring",
    "apply_when": "before pot_engine_v2 / mental_interpreter run (warmup planning)."
   }
  },
  {
   "id": "tool:github_skills",
   "kind": "runtime-tool",
   "name": "github_skills",
   "path": "scripts/github_skills.py",
   "description": "github_skills.py — GitHub-native skill discovery (trusted vendors + stars + semantic search). WHY THIS EXISTS: skills.sh is a convenient index (\"already filtered to what IS a skill\"), but it is a single network dependency that a locked-down",
   "triggers": "",
   "commands": [
    "python scripts/github_skills.py"
   ],
   "io": {
    "send": "ver docstring/CLI",
    "receive": "ver docstring",
    "apply_when": "As the marketplace tier of the discovery cascade when skills.sh is blocked/undesired, or to"
   }
  },
  {
   "id": "tool:gravity",
   "kind": "runtime-tool",
   "name": "gravity",
   "path": "scripts/gravity.py",
   "description": "gravity.py — Gravitational resource composition (APEX synergy engine). WHY THIS EXISTS: Picking one agent or one skill in isolation misses synergy. This engine treats every resource — scripts, agents, skills, diff-rules — as a body with MAS",
   "triggers": "",
   "commands": [
    "python scripts/gravity.py"
   ],
   "io": {
    "send": "a necessidade/tarefa (texto)",
    "receive": "constelação {agent, skill, script, diff} + gaps + pedidos STAGED",
    "apply_when": "resolver especialistas e recursos por disciplina"
   }
  },
  {
   "id": "tool:guards",
   "kind": "runtime-tool",
   "name": "guards",
   "path": "scripts/guards.py",
   "description": "guards.py — APEX inviolable guards SR_36..SR_40, made enforceable. Each guard is the literal rule from the APEX kernel, implemented as a check that returns a PASS/REJECT verdict (not LLM_BEHAVIOR hand-waving). Where a guard is purely about ",
   "triggers": "",
   "commands": [
    "python scripts/guards.py"
   ],
   "io": {
    "send": "ver docstring/CLI",
    "receive": "ver docstring",
    "apply_when": ""
   }
  },
  {
   "id": "tool:hypothesis_dag",
   "kind": "runtime-tool",
   "name": "hypothesis_dag",
   "path": "scripts/hypothesis_dag.py",
   "description": "hypothesis_dag.py — APEX hypothesis_dag (v00.32.1, OPP-MAS-01, SR_32), faithful port. Tracks dependencies between hypotheses, anchors, and findings. When an anchor is destroyed, invalidation cascades to all dependents via BFS O(V+E) with a ",
   "triggers": "",
   "commands": [
    "python scripts/hypothesis_dag.py"
   ],
   "io": {
    "send": "ver docstring/CLI",
    "receive": "ver docstring",
    "apply_when": ""
   }
  },
  {
   "id": "tool:learning",
   "kind": "runtime-tool",
   "name": "learning",
   "path": "scripts/learning.py",
   "description": "learning.py — APEX Op-P3: aprendizado que persiste (durable promote/demote loop). WHY THIS EXISTS: competence_matrix diagnoses a WEAK round in the moment (PERSONA_SWAP / INJECT_SKILL / HARD_PROBLEM) and competence.db holds per-persona signa",
   "triggers": "",
   "commands": [
    "python scripts/learning.py"
   ],
   "io": {
    "send": "record_outcome(kind, subject, domain, success)",
    "receive": "{mean, n, status PROMOTED/DEMOTED, changed}",
    "apply_when": "após cada rodada adjudicada / uso real de capacidade"
   }
  },
  {
   "id": "tool:llm_adapter",
   "kind": "runtime-tool",
   "name": "llm_adapter",
   "path": "scripts/llm_adapter.py",
   "description": "llm_adapter.py — APEX LLM Adapter (B2): the hardware-abstraction layer of the cognitive runtime. WHY THIS EXISTS: The kernel (SKILL.md + the 36 scripts) must stay stable while the model underneath changes — Claude, GPT, Gemini, or a small l",
   "triggers": "",
   "commands": [
    "python scripts/llm_adapter.py"
   ],
   "io": {
    "send": "ver docstring/CLI",
    "receive": "ver docstring",
    "apply_when": "At the start of a run on a NON-reference model, or whenever you need to know if a Level-B"
   }
  },
  {
   "id": "tool:local_discovery",
   "kind": "runtime-tool",
   "name": "local_discovery",
   "path": "scripts/local_discovery.py",
   "description": "local_discovery.py — LOCAL-first skill & MCP discovery (what is ALREADY installed here). WHY THIS EXISTS: The marketplace tiers (skills.sh, GitHub) look OUTWARD for skills to install. But the runtime usually ALREADY has skills on disk (`~/.",
   "triggers": "",
   "commands": [
    "python scripts/local_discovery.py"
   ],
   "io": {
    "send": "ver docstring/CLI",
    "receive": "ver docstring",
    "apply_when": "As tier 0 of discovery (before native index / skills.sh / GitHub). An installed skill that matches"
   }
  },
  {
   "id": "tool:memory",
   "kind": "runtime-tool",
   "name": "memory",
   "path": "scripts/memory.py",
   "description": "memory.py — APEX live cross-session vector memory (Op1): episodic + semantic, on SQLite. WHY THIS EXISTS: The snapshot lives in context and dies on interruption; the semantic index was built once at build-time. This is the live memory: an I",
   "triggers": "",
   "commands": [
    "python scripts/memory.py"
   ],
   "io": {
    "send": "remember(texto, kind) / recall(query) / relate(a, b, rel)",
    "receive": "sha da memória / top-k fatos / caminhada no grafo",
    "apply_when": "persistir e recuperar conhecimento validado"
   }
  },
  {
   "id": "tool:mental_interpreter",
   "kind": "runtime-tool",
   "name": "mental_interpreter",
   "path": "scripts/mental_interpreter.py",
   "description": "mental_interpreter.py — APEX mental_interpreter_v4 (v00.32.0, OPP-75), nativized core. WHAT IT IS: the execution orchestrator for DEEP/RESEARCH/SCIENTIFIC. It turns an intent into a sized, verified execution plan through four phases: SPECUL",
   "triggers": "",
   "commands": [
    "python scripts/mental_interpreter.py"
   ],
   "io": {
    "send": "ver docstring/CLI",
    "receive": "ver docstring",
    "apply_when": "DEEP/RESEARCH/SCIENTIFIC modes; SPECULATION only when fractal_depth>=2."
   }
  },
  {
   "id": "tool:menu",
   "kind": "runtime-tool",
   "name": "menu",
   "path": "scripts/menu.py",
   "description": "menu.py — The apex-method skill menu: update, choose preferred modes, deep research. WHY THIS EXISTS: A skill has no GUI, but users still want a small set of top-level actions. This is that menu, exposed as commands Claude runs on request: ",
   "triggers": "",
   "commands": [
    "python scripts/menu.py"
   ],
   "io": {
    "send": "ver docstring/CLI",
    "receive": "ver docstring",
    "apply_when": "When the user asks to update the skill, change preferred modes, or launch a deep/goal-like"
   }
  },
  {
   "id": "tool:monte_carlo",
   "kind": "runtime-tool",
   "name": "monte_carlo",
   "path": "scripts/monte_carlo.py",
   "description": "monte_carlo.py — Real Monte Carlo simulation (APEX monte_carlo_simulator, OPP-73), nativized. WHY THIS EXISTS: The skill forbids calling qualitative weighting \"Monte Carlo\" (§10 anti-pattern). This is the honest counterpart: when a hypothes",
   "triggers": "",
   "commands": [
    "python scripts/monte_carlo.py"
   ],
   "io": {
    "send": "model_fn(sample)->float + distribuições por input",
    "receive": "{P10/P50/P90, cv, CI, interpretation}",
    "apply_when": "incerteza quantificável (custo/risco/latência)"
   }
  },
  {
   "id": "tool:numeric",
   "kind": "runtime-tool",
   "name": "numeric",
   "path": "scripts/numeric.py",
   "description": "numeric.py — Deterministic numeric methods (APEX SCIENTIFIC mode). WHY THIS EXISTS: An LLM cannot integrate a differential equation by \"thinking\". Naive stepping (Euler) accumulates large error. RK4 gives orders-of-magnitude better accuracy",
   "triggers": "",
   "commands": [
    "python scripts/numeric.py"
   ],
   "io": {
    "send": "deriv(state)->d/dt, s0, dt, steps",
    "receive": "estado final (RK4/scipy) — validar contra quantidade conservada",
    "apply_when": "dinâmica/EDO multidimensional"
   }
  },
  {
   "id": "tool:orchestrator",
   "kind": "runtime-tool",
   "name": "orchestrator",
   "path": "scripts/orchestrator.py",
   "description": "orchestrator.py — The APEX flow, end to end. WHAT IT DOES (the flow you described): 1. EXPRESS CHECK — trivial input (2+2, one-line fact) skips the whole pipeline and answers directly. No bureaucracy. 2. DISSECT BY DISCIPLINE — a hard probl",
   "triggers": "",
   "commands": [
    "python scripts/orchestrator.py"
   ],
   "io": {
    "send": "a tarefa (string) [+ candidates, snapshot]",
    "receive": "{mode, specialists, kernel_checklist, llm_actions, gate}",
    "apply_when": "TODO início de trabalho não-trivial (ponto de entrada)"
   }
  },
  {
   "id": "tool:pipeline_dsm",
   "kind": "runtime-tool",
   "name": "pipeline_dsm",
   "path": "scripts/pipeline_dsm.py",
   "description": "pipeline_dsm.py — the Design Structure Matrix of the runtime itself (the author's item 3): map module coupling + the per-mode pipeline flow, and CUT token waste per execution mode. WHY THIS EXISTS: DSM is one of APEX's own validation framew",
   "triggers": "",
   "commands": [
    "python scripts/pipeline_dsm.py"
   ],
   "io": {
    "send": "ver docstring/CLI",
    "receive": "ver docstring",
    "apply_when": "build() after structural changes (new modules / new kernel steps); context_budget(mode) is"
   }
  },
  {
   "id": "tool:pot",
   "kind": "runtime-tool",
   "name": "pot",
   "path": "scripts/pot.py",
   "description": "pot.py — Program-of-Thought runner (APEX method). WHY THIS EXISTS: An LLM reasoning in prose approximates arithmetic and loses precision on multi-step numeric/logical work. PoT offloads those sub-problems to a real Python interpreter, so th",
   "triggers": "",
   "commands": [
    "python scripts/pot.py"
   ],
   "io": {
    "send": "passos [{name, code}] — o stdout de um passo vira stdin do próximo",
    "receive": "{ok, final_output, snapshot(checklist por passo)}",
    "apply_when": "qualquer subproblema com >2 passos numéricos/lógicos"
   }
  },
  {
   "id": "tool:project_ledger",
   "kind": "runtime-tool",
   "name": "project_ledger",
   "path": "scripts/project_ledger.py",
   "description": "project_ledger.py — The living project inventory (MACRO + micros) that survives session interruptions (usage limits, container recycling). WHY THIS EXISTS: Long projects lose state when a session is cut by a usage limit. The snapshot lives ",
   "triggers": "",
   "commands": [
    "python scripts/project_ledger.py"
   ],
   "io": {
    "send": "micros {who, what, to whom, depends_on}",
    "receive": "DSM (caminho crítico + lotes paralelos) + gate de conclusão",
    "apply_when": "projeto DEEP+ que atravessa sessões"
   }
  },
  {
   "id": "tool:rag_index",
   "kind": "runtime-tool",
   "name": "rag_index",
   "path": "scripts/rag_index.py",
   "description": "rag_index.py — SOLID-STATE node RAG: o índice semântico multidimensional do repositório com memórias cristalizadas, sync incremental e fusão de estados (a arquitetura do autor). WHY THIS EXISTS: Toda instância nova paga o \"imposto de remape",
   "triggers": "",
   "commands": [
    "python scripts/rag_index.py"
   ],
   "io": {
    "send": "pergunta PT/EN",
    "receive": "top nós {id, path, summary} para LER em seguida",
    "apply_when": "localizar qualquer coisa no repositório"
   }
  },
  {
   "id": "tool:repo_bridge",
   "kind": "runtime-tool",
   "name": "repo_bridge",
   "path": "scripts/repo_bridge.py",
   "description": "repo_bridge.py — Full APEX repository integration (on-demand, allowlisted). WHY THIS EXISTS: The skill ships catalogs (indexes), not the 16k-file APEX repo. Before this bridge, only 430 external theneoai skills were reachable and ZERO of th",
   "triggers": "",
   "commands": [
    "python scripts/repo_bridge.py"
   ],
   "io": {
    "send": "ver docstring/CLI",
    "receive": "ver docstring",
    "apply_when": "- `search_native(query)` at skill-resolution time (router step) to find a native"
   }
  },
  {
   "id": "tool:router",
   "kind": "runtime-tool",
   "name": "router",
   "path": "scripts/router.py",
   "description": "router.py — Semantic skill router (APEX skill-resolution). WHY THIS EXISTS: Given a task, decide WHICH skill (local or from the catalog) is most relevant, so the LLM loads specialized procedural knowledge on demand instead of guessing. WHEN",
   "triggers": "",
   "commands": [
    "python scripts/router.py"
   ],
   "io": {
    "send": "ver docstring/CLI",
    "receive": "ver docstring",
    "apply_when": "At the \"skill resolution\" step of the pipeline (STANDARD+), whenever a task"
   }
  },
  {
   "id": "tool:routine_composer",
   "kind": "runtime-tool",
   "name": "routine_composer",
   "path": "scripts/routine_composer.py",
   "description": "routine_composer.py — a persona compõe a PRÓPRIA ROTINA: um fluxo encadeado de capacidades complementares que se potencializam (o item 2 do autor, levado à execução). WHY THIS EXISTS: Saber que uma ferramenta existe não basta — o agente pre",
   "triggers": "",
   "commands": [
    "python scripts/routine_composer.py"
   ],
   "io": {
    "send": "ver docstring/CLI",
    "receive": "ver docstring",
    "apply_when": "- compose(challenge, agent_id): montar o fluxo para um desafio (o spawn injeta a rotina)."
   }
  },
  {
   "id": "tool:skill_forge",
   "kind": "runtime-tool",
   "name": "skill_forge",
   "path": "scripts/skill_forge.py",
   "description": "skill_forge.py — Native APEX skill generator (nativized from APEX tools/skill_forge.py). WHY THIS EXISTS: APEX's own skill-creation method, ported to the awesome-skills/neoformat schema so it runs STANDALONE (no APEX repo needed). Generates",
   "triggers": "",
   "commands": [
    "python3 skill_forge.py create --name my-skill --kind workflow --domain engineering \\\\",
    "python3 skill_forge.py promote --path path/to/SKILL.md"
   ],
   "io": {
    "send": "ver docstring/CLI",
    "receive": "ver docstring",
    "apply_when": ""
   }
  },
  {
   "id": "tool:skill_ledger",
   "kind": "runtime-tool",
   "name": "skill_ledger",
   "path": "scripts/skill_ledger.py",
   "description": "skill_ledger.py — remember MY choices: the provenance of every skill/tool decision. WHY THIS EXISTS: The runtime discovers skills, equips agents, and solves problems — but without a durable record of WHAT it chose and WHETHER it worked, eve",
   "triggers": "",
   "commands": [
    "python scripts/skill_ledger.py"
   ],
   "io": {
    "send": "ver docstring/CLI",
    "receive": "ver docstring",
    "apply_when": "Right after a skill/agent is used on a real problem (success OR failure). Call `record(...)`. At the"
   }
  },
  {
   "id": "tool:skill_scout",
   "kind": "runtime-tool",
   "name": "skill_scout",
   "path": "scripts/skill_scout.py",
   "description": "skill_scout.py — Discover, evaluate, and STAGE an external skill (safe). WHY THIS EXISTS: \"Find a skill on a directory -> evaluate -> know when to use it\" is useful, but silently installing/running internet code is a supply-chain risk. This",
   "triggers": "",
   "commands": [
    "python scripts/skill_scout.py"
   ],
   "io": {
    "send": "URL raw de um SKILL.md (+ code_urls)",
    "receive": "{status STAGED/REJECTED_UNSAFE, checks, snapshot_entry}",
    "apply_when": "SEMPRE antes de considerar uma skill externa (H5 depois)"
   }
  },
  {
   "id": "tool:skills_sh",
   "kind": "runtime-tool",
   "name": "skills_sh",
   "path": "scripts/skills_sh.py",
   "description": "skills_sh.py — skills.sh marketplace discovery with a popularity quality bar. WHY THIS EXISTS: The skill could search GitHub and the native library, but the richest source of battle-tested skills is the skills.sh leaderboard (Vercel's `npx ",
   "triggers": "",
   "commands": [
    "python scripts/skills_sh.py"
   ],
   "io": {
    "send": "ver docstring/CLI",
    "receive": "ver docstring",
    "apply_when": "At skill-resolution time, as the marketplace tier of the discovery cascade"
   }
  },
  {
   "id": "tool:snapshot",
   "kind": "runtime-tool",
   "name": "snapshot",
   "path": "scripts/snapshot.py",
   "description": "snapshot.py — Standardized session snapshot (APEX rule C5). WHY THIS EXISTS: Across a long task or a resumed conversation, the LLM needs a compact, fixed format state summary so it doesn't lose decisions, findings, or \"why\". The snapshot li",
   "triggers": "",
   "commands": [
    "python scripts/snapshot.py"
   ],
   "io": {
    "send": "ver docstring/CLI",
    "receive": "ver docstring",
    "apply_when": ""
   }
  },
  {
   "id": "tool:swap_store",
   "kind": "runtime-tool",
   "name": "swap_store",
   "path": "scripts/swap_store.py",
   "description": "swap_store.py — APEX swap store: one STANDARD, backend-agnostic memory hierarchy with versioned, revisioned, rotated files. WHY THIS EXISTS: The container is ephemeral, so working state (`~/.apex-method/*.db`, snapshots) dies on interruptio",
   "triggers": "",
   "commands": [
    "python scripts/swap_store.py"
   ],
   "io": {
    "send": "page_out(session, memory_db) / page_in_session(dir)",
    "receive": "bundle assinado + drive_manifest / stores restaurados",
    "apply_when": "fim de sessão DEEP+ (persist_due) / retomada (resume_due)"
   }
  },
  {
   "id": "tool:taxonomy",
   "kind": "runtime-tool",
   "name": "taxonomy",
   "path": "scripts/taxonomy.py",
   "description": "taxonomy.py — canonical (ENGLISH) facet classifier: the language-independent attraction layer. WHY THIS EXISTS: Lexical routing (`router` TF-IDF) fails across languages and on name collisions (\"moderna\"-> moderna-scientist, \"mobile\"->t-mobi",
   "triggers": "",
   "commands": [
    "python scripts/taxonomy.py"
   ],
   "io": {
    "send": "ver docstring/CLI",
    "receive": "ver docstring",
    "apply_when": ""
   }
  },
  {
   "id": "tool:token_tracker",
   "kind": "runtime-tool",
   "name": "token_tracker",
   "path": "scripts/token_tracker.py",
   "description": "token_tracker.py — medição REAL de tokens por rodada e por passo do kernel (OPP-99, item 3 do autor): as estimativas do fluxo por modo deixam de ser chute e passam a ser calibradas. WHY THIS EXISTS: pipeline_dsm.mode_flow decidia rodar/pula",
   "triggers": "",
   "commands": [
    "python scripts/token_tracker.py"
   ],
   "io": {
    "send": "ver docstring/CLI",
    "receive": "ver docstring",
    "apply_when": "- o orchestrator grava automaticamente (start_round/record/end_round em todo run FULL_PIPELINE);"
   }
  },
  {
   "id": "tool:uco_gate",
   "kind": "runtime-tool",
   "name": "uco_gate",
   "path": "scripts/uco_gate.py",
   "description": "uco_gate.py — Fast objective quality gate for generated code (APEX SR_33). WHY THIS EXISTS: Before running LLM-generated PoT code, check it with a DETERMINISTIC judge that does not depend on the LLM's opinion of its own code (breaks circula",
   "triggers": "",
   "commands": [
    "python scripts/uco_gate.py"
   ],
   "io": {
    "send": "código gerado (string)",
    "receive": "{status PASS/REJECTED, reasons, metrics(loop_risk, hamiltonian)}",
    "apply_when": "SEMPRE antes de executar código gerado (SR_33)"
   }
  },
  {
   "id": "tool:universal_code_optimizer_v4",
   "kind": "runtime-tool",
   "name": "universal_code_optimizer_v4",
   "path": "scripts/universal_code_optimizer_v4.py",
   "description": "Universal Code Optimizer (UCO) v4.0 ===================================== WHY: objective, LLM-independent code quality — 9-channel spectral analysis (Hamiltonian, cyclomatic, DSM, dead code, loop risk) so generated code is judged by math, n",
   "triggers": "",
   "commands": [
    "python scripts/universal_code_optimizer_v4.py"
   ],
   "io": {
    "send": "ver docstring/CLI",
    "receive": "ver docstring",
    "apply_when": "the SR_33 gate (uco_gate.py) calls this on every generated program before it runs."
   }
  },
  {
   "id": "tool:verification_gate",
   "kind": "runtime-tool",
   "name": "verification_gate",
   "path": "scripts/verification_gate.py",
   "description": "verification_gate.py — APEX verification_gate (v00.32.1, OPP-MAS-07), nativized. WHY: verifying is cheaper than generating (P≠NP insight), but verifying EVERY hypothesis in SCIENTIFIC can eat 25-33% of a 12k budget. The MCFE acts as a route",
   "triggers": "",
   "commands": [
    "python scripts/verification_gate.py"
   ],
   "io": {
    "send": "ver docstring/CLI",
    "receive": "ver docstring",
    "apply_when": "STEP_3, before interference. Disabled in EXPRESS/FAST/CLARIFY."
   }
  },
  {
   "id": "tool:verify",
   "kind": "runtime-tool",
   "name": "verify",
   "path": "scripts/verify.py",
   "description": "verify.py — Symbolic formal verifier (APEX C8 / SCIENTIFIC). WHY THIS EXISTS: Lets the LLM prove/refute an algebraic or calculus claim exactly, instead of asserting it. Crucially, it marks CONJECTURE when out of scope rather than inventing ",
   "triggers": "",
   "commands": [
    "python scripts/verify.py"
   ],
   "io": {
    "send": "lhs, rhs (expressões simbólicas)",
    "receive": "{tag FORMAL_VERIFIED/REFUTED/CONJECTURA_FORMAL, residual}",
    "apply_when": "qualquer identidade/derivada/integral afirmada"
   }
  },
  {
   "id": "skill:apex-method",
   "kind": "installed-skill",
   "name": "apex-method",
   "path": "/home/user/APEX/apex-method/tests/../SKILL.md",
   "description": "Token-aware reasoning workflow with real tools: picks an operating mode to control cost, runs a structured pipeline (decompose → validate → verify → snapshot), and gives Claude Program-of-Thought, RK4/Euler, a code gate, and a safe skill ro",
   "triggers": "APEX, PoT, program of thought, pipeline, scientific mode, modo científico, RK4, token",
   "commands": [
    "git COMMIT (the DISK of the RAM->SWAP->DISK hierarchy; push/PR stays a user action)."
   ],
   "io": {
    "send": "a tarefa/artefato descrito nos triggers do SKILL.md",
    "receive": "orientação/artefato conforme a seção de uso da skill",
    "apply_when": "multi-step or high-stakes tasks, real math, precise computation, audits, or the user mentions APEX, PoT, pipeline, or scientific mode.\""
   }
  },
  {
   "id": "skill:canvas-design",
   "kind": "installed-skill",
   "name": "canvas-design",
   "path": "/root/.claude/skills/canvas-design/SKILL.md",
   "description": "Create beautiful visual art in .png and .pdf documents using design philosophy. You should use this skill when the user asks to create a poster, piece of art, design, or other static piece. Create original visual designs, never copying exis",
   "triggers": "",
   "commands": [],
   "io": {
    "send": "a tarefa/artefato descrito nos triggers do SKILL.md",
    "receive": "orientação/artefato conforme a seção de uso da skill",
    "apply_when": "Create beautiful visual art in .png and .pdf documents using design philosophy. You should use this skill when the user asks to create a pos"
   }
  },
  {
   "id": "skill:apex-method",
   "kind": "installed-skill",
   "name": "apex-method",
   "path": "/root/.claude/skills/apex-method/SKILL.md",
   "description": "Token-aware reasoning workflow with real tools: picks an operating mode to control cost, runs a structured pipeline (decompose → validate → verify → snapshot), and gives Claude Program-of-Thought, RK4/Euler, a code gate, and a safe skill ro",
   "triggers": "APEX, PoT, program of thought, pipeline, scientific mode, modo científico, RK4, token",
   "commands": [
    "git COMMIT (the DISK of the RAM->SWAP->DISK hierarchy; push/PR stays a user action)."
   ],
   "io": {
    "send": "a tarefa/artefato descrito nos triggers do SKILL.md",
    "receive": "orientação/artefato conforme a seção de uso da skill",
    "apply_when": "multi-step or high-stakes tasks, real math, precise computation, audits, or the user mentions APEX, PoT, pipeline, or scientific mode.\""
   }
  },
  {
   "id": "skill:startup-hook-skill",
   "kind": "installed-skill",
   "name": "startup-hook-skill",
   "path": "/root/.claude/skills/session-start-hook/SKILL.md",
   "description": "Creating and developing startup hooks for Claude Code on the web. Use when the user wants to set up a repository for Claude Code on the web, create a SessionStart hook to ensure their project can run tests and linters during web sessions.",
   "triggers": "",
   "commands": [
    "npm install"
   ],
   "io": {
    "send": "a tarefa/artefato descrito nos triggers do SKILL.md",
    "receive": "orientação/artefato conforme a seção de uso da skill",
    "apply_when": "the user wants to set up a repository for Claude Code on the web, create a SessionStart hook to ensure their project can run tests and linte"
   }
  },
  {
   "id": "skill:slack-gif-creator",
   "kind": "installed-skill",
   "name": "slack-gif-creator",
   "path": "/root/.claude/skills/slack-gif-creator/SKILL.md",
   "description": "Knowledge and utilities for creating animated GIFs optimized for Slack. Provides constraints, validation tools, and animation concepts. Use when users request animated GIFs for Slack like \"make me a GIF of X doing Y for Slack.",
   "triggers": "",
   "commands": [
    "pip install pillow imageio numpy"
   ],
   "io": {
    "send": "a tarefa/artefato descrito nos triggers do SKILL.md",
    "receive": "orientação/artefato conforme a seção de uso da skill",
    "apply_when": "users request animated GIFs for Slack like \"make me a GIF of X doing Y for Slack.\""
   }
  },
  {
   "id": "skill:web-artifacts-builder",
   "kind": "installed-skill",
   "name": "web-artifacts-builder",
   "path": "/root/.claude/skills/web-artifacts-builder/SKILL.md",
   "description": "Suite of tools for creating elaborate, multi-component claude.ai HTML artifacts using modern frontend web technologies (React, Tailwind CSS, shadcn/ui). Use for complex artifacts requiring state management, routing, or shadcn/ui components ",
   "triggers": "",
   "commands": [
    "bash scripts/init-artifact.sh <project-name>",
    "bash scripts/bundle-artifact.sh"
   ],
   "io": {
    "send": "a tarefa/artefato descrito nos triggers do SKILL.md",
    "receive": "orientação/artefato conforme a seção de uso da skill",
    "apply_when": "Suite of tools for creating elaborate, multi-component claude.ai HTML artifacts using modern frontend web technologies (React, Tailwind CSS,"
   }
  },
  {
   "id": "skill:mcp-builder",
   "kind": "installed-skill",
   "name": "mcp-builder",
   "path": "/root/.claude/skills/mcp-builder/SKILL.md",
   "description": "Guide for creating high-quality MCP (Model Context Protocol) servers that enable LLMs to interact with external services through well-designed tools. Use when building MCP servers to integrate external APIs or services, whether in Python (F",
   "triggers": "",
   "commands": [],
   "io": {
    "send": "a tarefa/artefato descrito nos triggers do SKILL.md",
    "receive": "orientação/artefato conforme a seção de uso da skill",
    "apply_when": "building MCP servers to integrate external APIs or services, whether in Python (FastMCP) or Node/TypeScript (MCP SDK)."
   }
  },
  {
   "id": "skill:skill-creator",
   "kind": "installed-skill",
   "name": "skill-creator",
   "path": "/root/.claude/skills/skill-creator/SKILL.md",
   "description": "Create new skills, modify and improve existing skills, and measure skill performance. Use when users want to create a skill from scratch, edit, or optimize an existing skill, run evals to test a skill, benchmark skill performance with varia",
   "triggers": "",
   "commands": [
    "python -m scripts.aggregate_benchmark <workspace>/iteration-N --skill-name <name>",
    "python -m scripts.run_loop \\",
    "python -m scripts.package_skill <path/to/skill-folder>"
   ],
   "io": {
    "send": "a tarefa/artefato descrito nos triggers do SKILL.md",
    "receive": "orientação/artefato conforme a seção de uso da skill",
    "apply_when": "users want to create a skill from scratch, edit, or optimize an existing skill, run evals to test a skill, benchmark skill performance with "
   }
  },
  {
   "id": "skill:algorithmic-art",
   "kind": "installed-skill",
   "name": "algorithmic-art",
   "path": "/root/.claude/skills/algorithmic-art/SKILL.md",
   "description": "Creating algorithmic art using p5.js with seeded randomness and interactive parameter exploration. Use this when users request creating art using code, generative art, algorithmic art, flow fields, or particle systems. Create original algor",
   "triggers": "",
   "commands": [],
   "io": {
    "send": "a tarefa/artefato descrito nos triggers do SKILL.md",
    "receive": "orientação/artefato conforme a seção de uso da skill",
    "apply_when": "Creating algorithmic art using p5.js with seeded randomness and interactive parameter exploration. Use this when users request creating art "
   }
  },
  {
   "id": "skill:morning",
   "kind": "installed-skill",
   "name": "morning",
   "path": "/root/.claude/skills/morning/SKILL.md",
   "description": "Render the user's morning brief as a styled HTML artifact, or set it up as a recurring weekday task. Use only when the user explicitly asks to run, see, or set up their morning brief, or if they invoke /morning by name. A question about the",
   "triggers": "",
   "commands": [
    "npm pack @fontsource/fraunces",
    "node -e \"const{chromium}=require('playwright');(async()=>{const b=await chromium.launch({executablePath:'/opt/pw-browsers/chromium'});const p=await b.newPage({viewport:{width:960,height:1400}});await p.goto('file://<abs path>');await p.waitForTimeout(600);await p.screenshot({path:'brief.png',fullPage:true});await b.close();})();\""
   ],
   "io": {
    "send": "a tarefa/artefato descrito nos triggers do SKILL.md",
    "receive": "orientação/artefato conforme a seção de uso da skill",
    "apply_when": "Render the user's morning brief as a styled HTML artifact, or set it up as a recurring weekday task. Use only when the user explicitly asks "
   }
  },
  {
   "id": "skill:internal-comms",
   "kind": "installed-skill",
   "name": "internal-comms",
   "path": "/root/.claude/skills/internal-comms/SKILL.md",
   "description": "A set of resources to help me write all kinds of internal communications, using the formats that my company likes to use. Claude should use this skill whenever asked to write some sort of internal communications (status reports, leadership ",
   "triggers": "",
   "commands": [],
   "io": {
    "send": "a tarefa/artefato descrito nos triggers do SKILL.md",
    "receive": "orientação/artefato conforme a seção de uso da skill",
    "apply_when": "A set of resources to help me write all kinds of internal communications, using the formats that my company likes to use. Claude should use "
   }
  },
  {
   "id": "skill:brand-guidelines",
   "kind": "installed-skill",
   "name": "brand-guidelines",
   "path": "/root/.claude/skills/brand-guidelines/SKILL.md",
   "description": "Applies Anthropic's official brand colors and typography to any sort of artifact that may benefit from having Anthropic's look-and-feel. Use it when brand colors or style guidelines, visual formatting, or company design standards apply.",
   "triggers": "",
   "commands": [],
   "io": {
    "send": "a tarefa/artefato descrito nos triggers do SKILL.md",
    "receive": "orientação/artefato conforme a seção de uso da skill",
    "apply_when": "Applies Anthropic's official brand colors and typography to any sort of artifact that may benefit from having Anthropic's look-and-feel. Use"
   }
  },
  {
   "id": "skill:docx",
   "kind": "installed-skill",
   "name": "docx",
   "path": "/root/.claude/skills/docx/SKILL.md",
   "description": "Use this skill whenever the user wants to create, read, edit, or manipulate Word documents (.docx files) or Word templates (.dotx files). Triggers include: any mention of 'Word doc', 'word document', '.docx', '.dotx', or requests to produce",
   "triggers": "",
   "commands": [
    "python scripts/office/soffice.py --headless --convert-to pdf output.docx",
    "python scripts/merge_runs.py unpacked/   # coalesce fragmented runs so text is findable",
    "python scripts/office/validate.py out.docx --original doc.docx   # XSD checks; --auto-repair fixes common issues",
    "python scripts/comment.py unpacked/ \"Fees & expenses cap is too low\"",
    "python scripts/comment.py unpacked/ \"Agreed\" --parent 0",
    "python scripts/comment.py contract.docx \"This cap is too low\" -o annotated.docx"
   ],
   "io": {
    "send": "a tarefa/artefato descrito nos triggers do SKILL.md",
    "receive": "orientação/artefato conforme a seção de uso da skill",
    "apply_when": "extracting or reorganizing content from .docx or .dotx files, inserting or replacing images in documents, performing find-and-replace in Wor"
   }
  },
  {
   "id": "skill:theme-factory",
   "kind": "installed-skill",
   "name": "theme-factory",
   "path": "/root/.claude/skills/theme-factory/SKILL.md",
   "description": "Toolkit for styling artifacts with a theme. These artifacts can be slides, docs, reportings, HTML landing pages, etc. There are 10 pre-set themes with colors/fonts that you can apply to any artifact that has been creating, or can generate a",
   "triggers": "",
   "commands": [],
   "io": {
    "send": "a tarefa/artefato descrito nos triggers do SKILL.md",
    "receive": "orientação/artefato conforme a seção de uso da skill",
    "apply_when": "Toolkit for styling artifacts with a theme. These artifacts can be slides, docs, reportings, HTML landing pages, etc. There are 10 pre-set t"
   }
  },
  {
   "id": "skill:learn",
   "kind": "installed-skill",
   "name": "learn",
   "path": "/root/.claude/skills/learn/SKILL.md",
   "description": "|",
   "triggers": "",
   "commands": [],
   "io": {
    "send": "a tarefa/artefato descrito nos triggers do SKILL.md",
    "receive": "orientação/artefato conforme a seção de uso da skill",
    "apply_when": "|"
   }
  },
  {
   "id": "skill:xlsx",
   "kind": "installed-skill",
   "name": "xlsx",
   "path": "/root/.claude/skills/xlsx/SKILL.md",
   "description": "Use this skill any time a spreadsheet file is the primary input or output. This means any task where the user wants to: open, read, edit, or fix an existing .xlsx, .xlsm, .xltx, .csv, or .tsv file (e.g., adding columns, computing formulas, ",
   "triggers": "",
   "commands": [
    "python scripts/recalc.py output.xlsx [timeout_seconds]   # default 30"
   ],
   "io": {
    "send": "a tarefa/artefato descrito nos triggers do SKILL.md",
    "receive": "orientação/artefato conforme a seção de uso da skill",
    "apply_when": "Use this skill any time a spreadsheet file is the primary input or output. This means any task where the user wants to: open, read, edit, or"
   }
  },
  {
   "id": "skill:doc-coauthoring",
   "kind": "installed-skill",
   "name": "doc-coauthoring",
   "path": "/root/.claude/skills/doc-coauthoring/SKILL.md",
   "description": "Guide users through a structured workflow for co-authoring documentation. Use when user wants to write documentation, proposals, technical specs, decision docs, or similar structured content. This workflow helps users efficiently transfer c",
   "triggers": "",
   "commands": [],
   "io": {
    "send": "a tarefa/artefato descrito nos triggers do SKILL.md",
    "receive": "orientação/artefato conforme a seção de uso da skill",
    "apply_when": "user wants to write documentation, proposals, technical specs, decision docs, or similar structured content. This workflow helps users effic"
   }
  },
  {
   "id": "skill:pptx",
   "kind": "installed-skill",
   "name": "pptx",
   "path": "/root/.claude/skills/pptx/SKILL.md",
   "description": "Use this skill any time a .pptx or .potx file is involved in any way — as input, output, or both. This includes: creating slide decks, pitch decks, or presentations; reading, parsing, or extracting text from any .pptx or .potx file (even if",
   "triggers": "",
   "commands": [
    "python scripts/add_slide.py unpacked/ slide2.xml --after slide2.xml   # duplicate a slide (or slideLayoutN.xml); prints the new slide's path",
    "python scripts/clean.py unpacked/                                     # after deletions: removes orphaned slides, media, rels",
    "python scripts/office/validate.py out.pptx --original deck.pptx",
    "python scripts/office/validate.py output.pptx                      # built from scratch",
    "python scripts/office/validate.py output.pptx --original src.pptx  # built from a template",
    "python scripts/office/soffice.py --headless --convert-to pdf output.pptx"
   ],
   "io": {
    "send": "a tarefa/artefato descrito nos triggers do SKILL.md",
    "receive": "orientação/artefato conforme a seção de uso da skill",
    "apply_when": "Use this skill any time a .pptx or .potx file is involved in any way — as input, output, or both. This includes: creating slide decks, pitch"
   }
  },
  {
   "id": "skill:pdf",
   "kind": "installed-skill",
   "name": "pdf",
   "path": "/root/.claude/skills/pdf/SKILL.md",
   "description": "Use this skill whenever the user wants to do anything with PDF files. This includes reading or extracting text/tables from PDFs, combining or merging multiple PDFs into one, splitting PDFs apart, rotating pages, adding watermarks, creating ",
   "triggers": "",
   "commands": [],
   "io": {
    "send": "a tarefa/artefato descrito nos triggers do SKILL.md",
    "receive": "orientação/artefato conforme a seção de uso da skill",
    "apply_when": "Use this skill whenever the user wants to do anything with PDF files. This includes reading or extracting text/tables from PDFs, combining o"
   }
  },
  {
   "id": "template:laudo",
   "kind": "design-template",
   "name": "laudo",
   "path": "scripts/execution_policy.py",
   "description": "output template 'laudo' — required sections: ['director', 'ranking', 'best', 'confidence', 'justificativa', 'diagnostico', 'sha256']",
   "triggers": "",
   "commands": [],
   "io": {
    "send": "o rascunho da entrega",
    "receive": "documento estruturado nas seções ['director', 'ranking', 'best', 'confidence', 'justificativa', 'diagnostico', 'sha256']",
    "apply_when": "ao produzir uma entrega do tipo 'laudo'"
   }
  },
  {
   "id": "template:decision_record",
   "kind": "design-template",
   "name": "decision_record",
   "path": "scripts/execution_policy.py",
   "description": "output template 'decision_record' — required sections: ['context', 'options', 'decision', 'rationale', 'consequences', 'reversible?']",
   "triggers": "",
   "commands": [],
   "io": {
    "send": "o rascunho da entrega",
    "receive": "documento estruturado nas seções ['context', 'options', 'decision', 'rationale', 'consequences', 'reversible?']",
    "apply_when": "ao produzir uma entrega do tipo 'decision_record'"
   }
  },
  {
   "id": "template:spec",
   "kind": "design-template",
   "name": "spec",
   "path": "scripts/execution_policy.py",
   "description": "output template 'spec' — required sections: ['objective', 'scope', 'constraints', 'interfaces', 'acceptance_criteria', 'risks(FMEA)']",
   "triggers": "",
   "commands": [],
   "io": {
    "send": "o rascunho da entrega",
    "receive": "documento estruturado nas seções ['objective', 'scope', 'constraints', 'interfaces', 'acceptance_criteria', 'risks(FMEA)']",
    "apply_when": "ao produzir uma entrega do tipo 'spec'"
   }
  },
  {
   "id": "template:report",
   "kind": "design-template",
   "name": "report",
   "path": "scripts/execution_policy.py",
   "description": "output template 'report' — required sections: ['summary', 'evidence(what/where/how/confidence)', 'method', 'limitations', 'next']",
   "triggers": "",
   "commands": [],
   "io": {
    "send": "o rascunho da entrega",
    "receive": "documento estruturado nas seções ['summary', 'evidence(what/where/how/confidence)', 'method', 'limitations', 'next']",
    "apply_when": "ao produzir uma entrega do tipo 'report'"
   }
  },
  {
   "id": "template:apex_structure",
   "kind": "design-template",
   "name": "apex_structure",
   "path": "models/apex_structure.model.json",
   "description": "the canonical swap-tree/file-naming standard every backend must follow",
   "triggers": "",
   "commands": []
  }
 ]
}