---
skill_id: integrations.connectors.index
name: "Connectors Index — 17 Plugin Connectors"
description: "Index of all CONNECTORS.md files found. Each describes which external services a plugin can connect to via MCP."
version: v00.33.0
status: ADOPTED
domain_path: integrations/connectors
anchors:
  - connector
  - mcp
  - external_service
  - integration
  - plugin_connector
source_repo: multiple
risk: safe
languages: [dsl]
llm_compat: {claude: full, gpt4o: partial, gemini: partial, llama: minimal}
apex_version: v00.33.0
---

# Connectors Index

Total: **17 CONNECTORS.md** files documenting plugin integrations.

## Connectors Found

### partner-built

```
# Connectors

This plugin connects to the **LFA MCP Server**, which provides financial analytics tools from LSEG (London Stock Exchange Group). All tools are served by a single MCP server — no additio
```

### knowledge-work-plugins-main

```
# Connectors

## How tool references work

Plugin files use `~~category` as a placeholder for whatever tool the user connects in that category. For example, `~~literature` might mean PubMed, bioRxiv, 
```

### knowledge-work-plugins-main

```
# Connectors

## How tool references work

Plugin files use `~~category` as a placeholder for whatever tool the user connects in that category. For example, `~~support platform` might mean Intercom, Z
```

### knowledge-work-plugins-main

```
# Connectors

## How tool references work

Plugin files use `~~category` as a placeholder for whatever tool the user connects in that category. For example, `~~data warehouse` might mean Snowflake, Bi
```

### knowledge-work-plugins-main

```
# Connectors

## How tool references work

Plugin files use `~~category` as a placeholder for whatever tool the user connects in that category. For example, `~~design tool` might mean Figma, Sketch, o
```

### knowledge-work-plugins-main

```
# Connectors

## How tool references work

Plugin files use `~~category` as a placeholder for whatever tool the user connects in that category. For example, `~~source control` might mean GitHub, GitLa
```

### knowledge-work-plugins-main

```
# Connectors

## How tool references work

Plugin files use `~~category` as a placeholder for whatever tool the user connects in that category. For example, `~~chat` might mean Slack, Microsoft Teams,
```

### knowledge-work-plugins-main

```
# Connectors

## How tool references work

Plugin files use `~~category` as a placeholder for whatever tool the user connects in that category. For example, `~~data warehouse` might mean Snowflake, Bi
```

### knowledge-work-plugins-main

```
# Connectors

## How tool references work

Plugin files use `~~category` as a placeholder for whatever tool the user connects in that category. For example, `~~HRIS` might mean Workday, BambooHR, or a
```

### knowledge-work-plugins-main

```
# Connectors

## How tool references work

Plugin files use `~~category` as a placeholder for whatever tool the user connects in that category. For example, `~~cloud storage` might mean Box, Egnyte, o
```

### knowledge-work-plugins-main

```
# Connectors

## How tool references work

Plugin files use `~~category` as a placeholder for whatever tool the user connects in that category. For example, `~~marketing automation` might mean HubSpot
```

### knowledge-work-plugins-main

```
# Connectors

## How tool references work

Plugin files use `~~category` as a placeholder for whatever tool the user connects in that category. For example, `~~ITSM` might mean ServiceNow, Zendesk, or
```

### partner-built

```
# Connectors

## How tool references work

Plugin files use `~~category` as a placeholder for whatever tool the user connects in that category. This plugin is built around Common Room as the primary d
```

### knowledge-work-plugins-main

```
# Connectors

## Local MCP Server

This plugin uses a **local MCP server** instead of a remote connector.
The PDF server runs on your machine via `npx`.

| Category | Server | How it runs |
|---------
```

### knowledge-work-plugins-main

```
# Connectors

## How tool references work

Plugin files use `~~category` as a placeholder for whatever tool the user connects in that category. For example, `~~project tracker` might mean Linear, Asan
```

### knowledge-work-plugins-main

```
# Connectors

## How tool references work

Plugin files use `~~category` as a placeholder for whatever tool the user connects in that category. For example, `~~project tracker` might mean Asana, Linea
```

### knowledge-work-plugins-main

```
# Connectors

## How tool references work

Plugin files use `~~category` as a placeholder for whatever tool the user connects in that category. For example, `~~CRM` might mean Salesforce, HubSpot, or 
```

## Diff History
- **v00.33.0**: Ingested from CONNECTORS.md files
